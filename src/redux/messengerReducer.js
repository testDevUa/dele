let initialState = {
    messages: []
}

const messengerReducer = (state = initialState, action) => {
    switch (action.type) {
        default: return state;
    }
}

export default messengerReducer;